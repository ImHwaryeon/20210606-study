/**
 * for while(더빠름)
 * 
 * for of, for in => [], {}
 */

/**
 * if, else
 * swich
 * 
 * 조건이 3개이상 넘어가면 switch
 * 3개 아래면 if
 * 2개면 삼항연산자
 */

const bool1 = true
const bool2 = false
const bool3 = false

if(bool1) { // 순서대로 검사
}else if(bool2){
}else if(bool3){
}else{
}


const type = 'A'
switch(true){ // boolean 값을 넣어주면 조건식을 넣을수 있음
	case  'A' === 'B': {
	}
	case 'B' === 'C': {
	}
}
// 패턴매칭을 통해 순서대로가 아닌 바로 간다

/**
 * 삼항연산자
 */
